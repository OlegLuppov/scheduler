const BASE_URL_SCHEDULER = 'http://localhost:8081/'
const HEADER_USERS = 'users'
const HEADER_TASKS = 'tasks'

export const USERS = `${BASE_URL_SCHEDULER}${HEADER_USERS}`
export const TASKS = `${BASE_URL_SCHEDULER}${HEADER_TASKS}`
